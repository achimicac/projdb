<!DOCTYPE html>
<html lang="en">
<head>
      <meta charset="UTF-8">
      <meta name="viewport" content="width=device-width, initial-scale=1.0">
      <title>Document</title>
      <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
      <style>
            .Allpet{
                  display: flex;
                  flex-direction: row;
                  flex-wrap: wrap;
            }
            #pet{
                  margin: 20px;
            }
            .container {
                  align-items: center;
                  /*background-color: #f9f9f9;*/
                  display: flex;
                  justify-content: center;
                  /*max-width: 100%;*/
                  position: fixed;
                  bottom: 30px;
            }
            a.add-to-cart {
                  background-color: #0dd;
                  border: none;
                  transition:
                        background-color .5s linear,
                        color .5s linear
                  ;
            
                  &:hover { background-color: darken(#0dd, 5%); }
                  &:focus { outline: none; }
                  &:active { background-color: rgb(10, 145, 145);}
            }
            .add-to-cart {
                  animation: shadowFloat 1.2s linear alternate infinite;
                  border-radius: 50px;
                  color: #fff;
                  font-size: 13px;
                  padding: 20px 40px;
                  text-transform: uppercase;
                  text-shadow: 0 1px 1px darken(#0dd, 10%);
            }            
            @keyframes shadowFloat {
                  0%   {
                           box-shadow: 0 20px 40px #c7c7c7;
                           transform: translateY(-3px);
                     }
                  100% {
                           box-shadow: 0 10px 10px darken(#c7c7c7, 5%);;
                           transform: translateY(3px); 
                     }
            }
      </style>
</head>
<body>
      <!-- Show All pet-->
      <div class="Allpet">
            <% if (all_pet.length != 0) { %>
                  <% for (let i = 0; i < all_pet.length; i++) { %>
                        <div class="card" style="width: 18rem;" id="pet">
                              <!-- <img src="..." class="card-img-top" alt="..."> -->
                              <div class="card-body">
                                    <h5 class="card-title"> <%= all_pet[i].petName %> </h5>
                                    <p class="card-text"> Pet type :  <%= all_pet[i].petType %> </p>
                                    <p class="card-text"> Date of Birth : <%= all_pet[i].petDoB %> </p>
                                    <p class="card-text"> Age year: <%= all_pet[i].years %> </p>
                                    <p class="card-text"> Age month: <%= all_pet[i].weeks %>
                                    <p class="card-text"> Age week: <%= all_pet[i].months %>
                                    <p class="card-text"> Age day: <%= all_pet[i].days %>
                                    <p class="card-text"> Gender :  <%= all_pet[i].petGender %> </p>

                                    <a href="/petprofile/<%= all_pet[i].petID %>" class="btn btn-primary"> Profile </a>
                              </div>
                        </div>
                  <% } %>
            <% } else { %>
                  <h1> You don't have a pet </h1>
            <% } %>
      </div>
      <div class="container">
            <a href="/calendar" class="add-to-cart"> CALENDAR </a>
      </div>
</body>
</html>